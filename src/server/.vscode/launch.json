{
	"version": "0.1.0",
	// List of configurations. Add new configurations or edit existing ones.
	"configurations": [
		{
			"name": "Attach",
			"type": "node",
			"request": "attach",
            "stopOnEntry":true,
            "localRoot": "${workspaceRoot}",
			"port": 5004
		}
	]
}